package test.application.pages;/*
package TestFunctions;

import Util.GlobalVars;

import javax.security.auth.login.Configuration;

public class SystemNavigator {


    public static boolean NavigateToPage(String PageName)
    {
        boolean res = false;
        try
        {


            // SystemLoging.langSwitch(GlobalVars.Test.RunLanguage)
            GlobalVars.browser.get(PageName);
            res = true;

        }
        catch (Exception ex)
        {
            Logger.LogError(ex);
            ReporterX.ReportEvent("System Navigation.", "go to Create User Page Failed For : " + ex.Message, Status.FAILED);

        }
        return res;
    }

}}
*/
